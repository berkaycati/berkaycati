## first try

def combine_names(name, surname):
    return ' '.join([name, surname])

def name_surname_input():
    name = str(input('Your name: '))
    surname = str(input('Surname: '))
    combine_names(name,surname)
    

name = str(input('Your name: '))
surname = str(input('Surname: '))
    
while name == '':
    print('Enter a valid name.')
    name = str(input('Your name: '))
    continue
    
    
while soyisim == '':
    print('Enter a valid surname.')
    surname = str(input('Surname: ')) 
    continue 
    
system_name = combine_names(name, surname)
print('You have entered your information into the system as {}*, welcome!'.format(system_name))

## python ##
